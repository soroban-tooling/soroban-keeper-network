node_modules/
dist/

# package-lock.json IS committed (unlike the batch-register example package)
# — this package's CI job (sdk-ts-docs) depends on typedoc resolving
# reproducibly, so an unpinned lockfile-less install is a real risk here,
# not just a style preference.

# Generated by `npm run docs` (see typedoc.json) — a build artifact, not
# source. `npm run docs:check` (wired into CI) validates it can be
# regenerated without warnings; it is not published to this repo.
docs/reference/
dist-test/
